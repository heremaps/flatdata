namespace n {
const i8 INVALID_ZERO = 0;
}

namespace n {
const i8 INVALID_MIN_INT = -128;
}

namespace n {
const i8 INVALID_MAX_INT = 127;
}

namespace n {
struct Bar
{
    @invalid_value( .n.INVALID_ZERO )
    invalid_zero : i8 : 8;
    @invalid_value( .n.INVALID_MIN_INT )
    invalid_min_int : i8 : 8;
    @invalid_value( .n.INVALID_MAX_INT )
    invalid_max_int : i8 : 8;
}
}

